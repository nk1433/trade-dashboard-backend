# Skills index — trade-dashboard-backend

Read this file before loading any skill. Load **one** matching `SKILL.md` only.

| Skill | When to use | Path |
|-------|-------------|------|
| `trade-api` | Routes, dbWrapper, dual-DB schema, scans | `.cursor/skills/trade-api/SKILL.md` |
| `upstox-backend` | OAuth, WebSocket, cron, market data | `.cursor/skills/upstox-backend/SKILL.md` |

Trae mirror: `.trae/skills/<name>/SKILL.md`

Commands: `/find-context`, `/review-staged`
